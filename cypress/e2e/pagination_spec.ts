describe("pagination", () => {
  it("test if pagination works properly", () => {});
  // on load user is on page one of all results
  // check if prev is disabled
  // click on next for 1-10 times
  // click on any page except last one
  // go to the last page
  // check if next is disbled
};);
